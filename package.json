{
  "name": "@dcolesdev/lerna-npm",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "commit": "cz",
    "test:all": "lerna run test",
    "ci:install": "yarn install --frozen-lockfile",
    "ci:changes": "lerna ls --since",
    "ci:build": "lerna run build --since",
    "ci:test": "lerna run test --since",
    "ci:version": "lerna version --conventional-commits --yes",
    "ci:publish": "lerna publish -y",
    "ci:docs": "node scripts/docs",
    "styleguide:dev": "styleguidist server",
    "styleguide:build": "styleguidist build"
  },
  "devDependencies": {
    "@babel/cli": "^7.13.14",
    "babel-loader": "^8.2.2",
    "commitizen": "^4.2.3",
    "cz-conventional-changelog": "^3.3.0",
    "enzyme": "^3.11.0",
    "eslint": "^7.24.0",
    "husky": "^6.0.0",
    "jest": "^26.6.3",
    "lerna": "^4.0.0",
    "lint-staged": "^10.5.4",
    "markdown-magic": "^2.0.0",
    "postcss-cli": "^7.1.2",
    "prettier": "^2.2.1",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-styleguidist": "^11.1.6",
    "react-test-renderer": "^17.0.2",
    "rimraf": "^3.0.2",
    "rollup": "^2.45.2",
    "sync-rpc": "^1.3.6",
    "webpack": "^5.33.2"
  },
  "husky": {
    "hooks": {
      "prepare-commit-msg": "exec < /dev/tty && git cz --hook || true",
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "./**/*.{js,jsx,ts,tsx}": [
      "eslint --fix && prettier --write"
    ]
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
